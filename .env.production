# Production Environment Variables
# Note: Sensitive values should be set in Netlify environment variables

NODE_ENV=production
PORT=3000

# API URLs for production
REACT_APP_API_URL=https://physics-api.railway.app/api
REACT_APP_SERVER_URL=https://physics-api.railway.app

# Client URL for production
CLIENT_URL=https://physics-mini-app-v2.netlify.app

# Database (will use production database)
DATABASE_URL=sqlite:///physics_app_production.db

# Secrets (set these in Netlify environment variables, not in code)
# BOT_TOKEN=<set-in-netlify-env>
# JWT_SECRET=<set-in-netlify-env>
